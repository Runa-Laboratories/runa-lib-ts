# First session

Create the client, create a session, perform work, delete the session, and
always close the client. The canonical runnable source is
[`examples/guides/first-session.ts`](../../examples/guides/first-session.ts).
