# Session lifecycle

Use `start`, `pause`, `resume`, `stop`, or `refresh` on a client-owned
`Session`. Each successful call updates the same handle.

Source: [`examples/guides/session-lifecycle.ts`](../../examples/guides/session-lifecycle.ts).
