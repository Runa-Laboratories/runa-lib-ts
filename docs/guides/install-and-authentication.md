# Install and authentication

Install with `npm install @runa_laboratories/sdk`. Set `RUNA_API_KEY` in the process
environment, or provide `apiKey` to `new Runa(...)`. Never put an API key in
source control or logs. Node.js 22 or newer and ESM are required.
